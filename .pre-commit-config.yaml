repos:
  - repo: local
    hooks:
      # gitleaks より先に同期する。後だと今回 stage した配信用コピーが secret scan から外れる。
      - id: sync-external-skills
        name: Sync lock-tracked skills to publish dir
        entry: node scripts/sync-external-skills.mjs --apply --stage
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
      - id: gitleaks-pre-commit
        name: Detect hardcoded secrets
        entry: gitleaks git --pre-commit --redact --staged --verbose
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
      - id: gitleaks-pre-push
        name: Detect hardcoded secrets (push)
        entry: gitleaks git --redact --verbose
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]
