# description と comment のリンク

PR や issue の description、comment、reply などに URL を持つもの（issue や commit など）を記載するときは、可能な限りリンク形式にする。

## 手順

1. 記載対象に issue、PR、commit、discussion などの URL があるか確認する
1. Markdown リンク `[表示名](URL)` にする
1. 生 URL の併記はしない
